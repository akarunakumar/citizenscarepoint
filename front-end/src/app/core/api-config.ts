/**
 * api-config.ts
 * The one place the backend URL is defined — same role config.js played
 * in the vanilla JS version. Change this single line when the backend
 * moves to Azure/Render/wherever in a later phase.
 */
export const API_BASE_URL = 'http://localhost:8080';
